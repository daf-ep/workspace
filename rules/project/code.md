# Customize the Sharing Threshold

This file repeats nothing from `common/code.md`: why a duplicate costs, what a function loses by moving into shared code, and how to organize the shared place stay defined there, once and for all. This file only says how many uses **this particular project** tolerates before sharing.

By default, no exception to declare: the project follows `code.md` as is. Replacing "No exception, see code.md" with what this project actually does is all it takes to adapt it.

## Threshold

No exception, see `code.md`.
