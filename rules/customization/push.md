# Customize the Commit Message

This file repeats nothing from `../common/push.md`: splitting into scopes, finding a change's real extent, and the whole mechanism stay defined there, once and for all, identical from one project to the next. This file only says what **this particular project** does differently to write the message text.

By default, no section has an exception to declare: the project follows `push.md` as is. Replacing "No exception, see push.md" with what this project actually does is all it takes to adapt it.

## Tags

No exception, see `push.md`.

## Format

No exception, see `push.md`.

## Language

No exception, see `push.md`.

## Message Footer

No exception, see `push.md`. Never covers the attribution ban: it stays outside any customization, whatever this project writes here.
