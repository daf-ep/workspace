name: cli
description: dpw, the command-line tool that syncs a project's rules and context for dafep.
version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:
  args: ^2.7.0
  dart_mcp: ^0.5.2
  fiber_shell: ^1.4.0
  path: ^1.9.0
  sqlite3: ^3.5.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
