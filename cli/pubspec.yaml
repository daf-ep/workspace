name: cli
description: The dafep command-line tool, run inside a project to sync its rules and context.
version: 0.1.0

environment:
  sdk: ^3.12.2

dependencies:
  args: ^2.7.0
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
